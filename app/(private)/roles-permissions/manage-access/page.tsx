import React from "react"
import Page from "@/components/layouts/Page"
import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Manage Access",
  description: "Manage Access",
}

const page = () => {
  return <Page title="Manage Access">Manage Access</Page>
}

export default page
